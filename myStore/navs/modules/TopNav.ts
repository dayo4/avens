// import { $Notify, $Axios, $LSAgent, $Router } from '@/plugins'

export class TopNav {

    element: HTMLElement = null  /* This will be used to point to the TopNav component from other components */

    // show(query?: { showQuery: boolean, message?: string, redirect?: string }) {
    //     if (query)
    //     {
    //         this.query = query as any
    //     }

    //     this.status = true
    // }

}
